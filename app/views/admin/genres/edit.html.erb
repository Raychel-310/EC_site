<h1>管理者#ジャンル編集</h1>

<h2>ジャンル一覧・追加</h2>

<%= form_with model: @genre, url: admin_genre_path, method: :patch do |f| %>
  <h4>ジャンル名</h4>
  <%= f.text_field :name %>
  <%= f.submit "変更を保存" %>
<% end %>

<!--<%# redirect_to admin_genres_path %>-->